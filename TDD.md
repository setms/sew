# TDD

## Test list

- [x] `Docker.applySuggestion()` should create `docker-compose.yml` in addition to `Dockerfile.
  This Docker Compose file should contain instruction to build the app, under the `include-app` profile.
- [x] `Docker.applySuggestion()` should add the PostgreSql container to `docker-compose.yml` if the `Database` decision
  equals `PostgreSql`.
- [x] The `docker-compose.yml` generated by `Docker.applySuggestion()` should work.
  Currently, it gives:
  ```shell
  docker compose up
  [+] up 18/18
  ✔ Image postgres           Pulled                                                                                                                       15.0s
  ✔ Network e2e_default      Created                                                                                                                      0.0s
  ✔ Container e2e-postgres-1 Created                                                                                                                      0.3s
  Attaching to postgres-1
  postgres-1  | Error: Database is uninitialized and superuser password is not specified.
  postgres-1  |        You must specify POSTGRES_PASSWORD to a non-empty value for the
  postgres-1  |        superuser. For example, "-e POSTGRES_PASSWORD=password" on "docker run".
  postgres-1  |
  postgres-1  |        You may also use "POSTGRES_HOST_AUTH_METHOD=trust" to allow all
  postgres-1  |        connections without a password. This is *not* recommended.
  postgres-1  |
  postgres-1  |        See PostgreSQL documentation about "trust":
  postgres-1  |        https://www.postgresql.org/docs/current/auth-trust.html
  postgres-1 exited with code 1
  ```
- [~] The `docker-compose.yml` generated by `Docker.applySuggestion()` should include a health check for PostgreSql.
  The name of the service shouldn't be `postgres`, but `db`.
  The application service should require the `db` service to be up, **if there is one**.
- [x] `AggregateTool` should extend `DtoCodeTool`, like `CommandTool` does.
- [ ] `AggregateTool.validate()` should warn about missing domain object for the aggregate, just like it does for the
  domain service.
  This diagnostic should contain a suggestion to create the missing domain object.
- [ ] `AggregateTool.doApply()` should handle the suggestion to create a missing domain object.
