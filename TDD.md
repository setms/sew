# TDD

## Test list

- [ ] `SpringBootCodeGenerator.generateControllerFor` needs to add the dependency
  `org.springframework.boot:spring-boot-starter-web`.
- [ ] Find all callers of `Resource.writeTo()` and check if they should use `Resource.writeAsString()` instead.
