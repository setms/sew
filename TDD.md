# TDD

## Test list

- [~] `BuildConfigurationTool` should not require build configuration when no unit tests are present
- [ ] `BuildConfigurationTool` should require build configuration when unit tests are present but build files are absent
