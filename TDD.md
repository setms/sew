# TDD

## Test list

- [x] The `docker-compose.yml` generated by `Docker.applySuggestion()` should set the active spring profile
  of the application to `local` via an environment variable.
- [x] `build.gradle` generated by `SpringBootCodeGenerator` should contain `runtimeOnly 'org.postgresql:postgresql'` if
  the `Database` decision is `PostgreSql`.
- [x] The `application-local.yml` generated by `SpringBootCodeGenerator` needs `password` and `username` properties for
  the data source.
- [x] `Docker` must set these options on the `db` service in `docker-compose.yml`:
  ```plaintext
  POSTGRES_DB: todo
  ports:
  - "5432:5432"
  ```
  where `todo` comes from the `applicationName` field.
- [~] `Docker` must set this on the `app` service in `docker-compose.yml`:
  `SPRING_DATASOURCE_URL: jdbc:postgresql://db:5432/todo`,
  where `todo` comes from the `applicationName` field.
- [ ] `AggregateTool.validate()` should warn about missing domain object for the aggregate, just like it does for the
  domain service.
  This diagnostic should contain a suggestion to create the missing domain object.
- [ ] `AggregateTool.doApply()` should handle the suggestion to create a missing domain object.
