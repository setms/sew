plugins {
    id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}

rootProject.name = 'sew'

include('core', 'intellij-lang-sew', 'intellij-lang-acceptance', 'intellij', 'web', 'examples:gdpr', 'examples:throughput-stats-collector')
