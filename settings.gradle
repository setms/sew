plugins {
    id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}

rootProject.name = 'sew'

include('km', 'swe', 'intellij-lang-sal', 'intellij-lang-acceptance', 'intellij', 'softure', 'examples:gdpr-middleware')
