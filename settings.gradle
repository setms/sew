plugins {
    id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}

rootProject.name = 'sew'

include('core', 'intellij-lang-sal', 'intellij-lang-acceptance', 'intellij', 'web', 'examples:gdpr', 'examples:throughput-stats-collector')
