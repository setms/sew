package sew.backend

decision CodeOrganization {
    topic     = codeOrganization
    choices   = [ "OnionArchitecture" ]
    rationale = "Keep business logic separated from frameworks and external dependencies like databases"
}
