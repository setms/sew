package gdpr

useCase UpdateUsers {
    title = "Update users"
    description = "Apps informs us of changes in their users."
}

scenario UpdateUsers {
    steps = [
        externalSystem(App),
        event(UserUpdated),
        readModel(Users)
    ]
}
