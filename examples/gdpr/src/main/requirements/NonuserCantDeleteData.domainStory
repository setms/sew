package gdpr

domainStory NonuserCantDeleteData {
    description = "When someone who's not a user of our applications requests data deletion, customer support denies the request."
}

sentence {
    parts = [
        user(Nonuser),
        activity(Requests),
        workObject(DataDeletion, icon=material(Delete))
    ]
}

sentence {
    parts = [
        user(CustomerSupportAgent),
        activity(Checks),
        workObject(Request, icon=material(Draft)),
        activity(Against),
        workObject(Users, icon=material(People))
    ]
}

sentence {
    parts = [
        user(CustomerSupportAgent),
        activity(Sends),
        workObject(DeniedRequest, icon=material(Draft)),
        activity(To),
        externalSystem(GdprMiddleware)
    ]
}
