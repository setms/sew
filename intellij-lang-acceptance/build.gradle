plugins {
    id 'java'
    alias libs.plugins.intellij.platform
    alias libs.plugins.intellij.grammarkit
}

repositories {
    intellijPlatform {
        defaultRepositories()
    }
}

dependencies {
    intellijPlatform {
        intellijIdeaCommunity("2024.3.5")
        instrumentationTools()
        pluginVerifier()
    }
}

generateLexer {
    sourceFile = file('src/main/grammar/Acceptance.flex')
    targetOutputDir = file('build/generated/sources/flex/org/setms/sew/intellij/lang/acceptance')
    purgeOldFiles = true
}
sourceSets.main.java.srcDir 'build/generated/sources/flex'

generateParser {
    sourceFile = file('src/main/grammar/Acceptance.bnf')
    targetRootOutputDir = file('build/generated/sources/bnf')
    // These are required, but seem to be unused if the .bnf provides the FQNs
    pathToParser = 'parser'
    pathToPsiRoot = 'psi'
    purgeOldFiles = true
}
sourceSets.main.java.srcDir 'build/generated/sources/bnf'

compileJava.dependsOn generateParser, generateLexer
