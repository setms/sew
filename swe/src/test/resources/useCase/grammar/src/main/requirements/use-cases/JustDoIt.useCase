package valid

useCase JustDoIt {
    title = "Just do it"
}

scenario HappyPath {
    steps = [
        user(Duck),
        event(ItWasDone),
        aggregate(System),
        command(DoIt),
        policy(WheneverItWasDone)
    ]
}
