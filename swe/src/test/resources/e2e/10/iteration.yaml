outputs:
  - .gitattributes
  - .gitignore
  - build.gradle
  - gradle.properties
  - gradlew
  - gradlew.bat
  - gradle/libs.versions.toml
  - gradle/wrapper/gradle-wrapper.jar
  - gradle/wrapper/gradle-wrapper.properties
  - settings.gradle

diagnostics:
  - static import only from classes and interfaces
  - static import only from classes and interfaces
  - cannot find symbol
  - cannot find symbol
  - cannot find symbol
  - cannot find symbol
  - package com.company.todo.domain.model does not exist
  - package com.company.todo.domain does not exist
  - package com.company.todo.domain does not exist
