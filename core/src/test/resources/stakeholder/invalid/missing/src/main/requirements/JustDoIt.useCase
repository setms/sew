package nonuser

useCase JustDoIt {
    title = "Just do it"
}

scenario HappyPath {
    title = "All's well that ends well"
    steps       = [
        user(Micky),
        command(DoIt),
        aggregate(System)
    ]
}
