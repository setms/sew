package gdpr

domainStory NonuserCantDeleteData {
    description = "A person who isn't a user can't delete data."
}

sentence Sentence1 {
    parts = [
        user(Nonuser),
        activity(Requests),
        workObject(DataDeletion, icon=material(Delete))
    ]
}

sentence Sentence2 {
    parts = [
        user(CustomerSupportAgent),
        activity(Checks),
        workObject(Request, icon=material(Delete)),
        activity(Against),
        workObject(Users, icon=material(People))
    ]
}

sentence Sentence3 {
    parts = [
        user(CustomerSupportAgent),
        activity(Rejects),
        workObject(Request, icon=material(Delete))
    ]
}
