package valid

useCase JustDoIt {
    display = "Just do it"
}

scenario HappyPath {
    description = "All's well that ends well"
    steps       = [
        user(Duck),
        command(DoIt),
        aggregate(System)
    ]
}
